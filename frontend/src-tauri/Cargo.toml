[package]
name = "papertrans-desktop"
version = "0.1.0"
description = "PaperTrans desktop shell"
authors = ["PaperTrans contributors"]
edition = "2021"

[lib]
name = "papertrans_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
uuid = { version = "1", features = ["v4"] }
